<%= form_with(model: current_user.bookmarks.find_by(note_id: note.id), method: :delete, remote: true) do |f| %>
  <%= f.button class: "bookmarked" do %>
    <i class="fas fa-bookmark"></i>
  <% end %>
<% end %>

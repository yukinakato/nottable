<%= form_with(model: current_user.bookmarks.find_by(note_id: note.id), method: :delete, local: true) do |f| %>
  <%= f.button class: "bookmarked note-tool-icon" do %>
    <i class="fas fa-bookmark"></i>
  <% end %>
<% end %>

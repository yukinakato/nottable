<%= form_with(model: current_user.bookmarks.build, local: true) do |f| %>
  <%= hidden_field :bookmark, :user_id, value: current_user.id %>
  <%= hidden_field :bookmark, :note_id, value: note.id %>
  <%= f.button do %>
    <i class="far fa-star"></i>
  <% end %>
<% end %>

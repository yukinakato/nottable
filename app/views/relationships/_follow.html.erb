<%= form_with(model: current_user.active_relationships.build, local: true) do |f| %>
  <%= hidden_field_tag :followed_id, user.id %>
  <%= f.button do %>
    <i class="far fa-bell"></i>&nbsp;未フォロー
  <% end %>
<% end %>

<%= form_with(model: current_user.active_relationships.find_by(followed_id: user.id),
              method: :delete, remote: true) do |f| %>
  <%= f.button class: "btn stop-follow" do %>
    <i class="far fa-bell-slash"></i>&nbsp;アンフォロー
  <% end %>
<% end %>
